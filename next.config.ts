import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: pdf-parse and mammoth may require additional configuration
  // for serverless environments. Consider using external services
  // or edge-compatible alternatives if issues arise.
};

export default nextConfig;
